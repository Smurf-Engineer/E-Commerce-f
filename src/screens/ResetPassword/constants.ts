/**
 * ResetPassword action types
 */

const namespace = 'src/ResetPassword'

export const DEFAULT_ACTION = `${namespace}/DEFAULT_ACTION`

export const SET_PASSWORD = `${namespace}/SET_PASSWORD`
export const SET_CONFIRM_PASSWORD = `${namespace}/SET_CONFIRM_PASSWORD`
