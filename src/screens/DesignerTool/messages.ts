/**
 * DesignerTool -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  title: {
    id: 'screens.DesignerTool.tittle',
    defaultMessage: 'DesignerTool'
  }
})