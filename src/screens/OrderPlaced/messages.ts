/**
 * OrderPlaced -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  title: {
    id: 'screens.OrderPlaced.title',
    defaultMessage: 'YOUR ORDER HAS BEEN PLACED!'
  }
})
