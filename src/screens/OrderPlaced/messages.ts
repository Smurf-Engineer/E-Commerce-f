/**
 * OrderPlaced -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  title: {
    id: 'screens.OrderPlaced.tittle',
    defaultMessage: 'OrderPlaced'
  }
})