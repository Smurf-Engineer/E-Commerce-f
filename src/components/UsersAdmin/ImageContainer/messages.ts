/**
 * ImageContainer -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  delete: {
    id: 'components.UsersAdmin.ImageContainer.delete',
    defaultMessage: 'Delete'
  }
})
