/**
 * InspirationModal -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  select: {
    id: 'components.InspirationModal.select',
    defaultMessage: 'Select'
  }
})
