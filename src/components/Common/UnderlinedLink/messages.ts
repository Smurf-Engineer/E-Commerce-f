/**
 * UnderlinedLink -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  title: {
    id: 'components.UnderlinedLink.tittle',
    defaultMessage: 'UnderlinedLink'
  }
})