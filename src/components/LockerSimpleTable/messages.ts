/**
 * LockerSimpleTable -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  name: {
    id: 'components.LockerSimpleTable.name',
    defaultMessage: 'Name'
  }
})
