/**
 * ImageSlider -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  title: {
    id: 'components.ImageSlider.tittle',
    defaultMessage: 'ImageSlider'
  }
})