/**
 * PalettesList -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  emptyPalettes: {
    id: 'components.MyFiles.emptyPalettes',
    defaultMessage: 'No palettes, yet'
  }
})
