/**
 * SearchBar - messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  hint: {
    id: 'components.searchBar.hint',
    defaultMessage: 'Search for a product'
  }
})
