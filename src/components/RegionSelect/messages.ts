/**
 * RegionSelect -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  title: {
    id: 'components.RegionSelect.tittle',
    defaultMessage: 'RegionSelect'
  },
  select: {
    id: 'components.RegionSelect.tittle',
    defaultMessage: 'Select state/province'
  }
})
