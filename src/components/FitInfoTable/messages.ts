/**
 * FitInfoTable -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  title: {
    id: 'components.FitInfoTable.tittle',
    defaultMessage: 'FitInfoTable'
  }
})