/**
 * CartForHeader -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  title: {
    id: 'components.CartForHeader.tittle',
    defaultMessage: 'CartForHeader'
  }
})