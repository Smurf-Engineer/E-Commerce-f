/**
 * ColorButtons -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  area: {
    id: 'components.designCenterCustomize.ColorButtons.area',
    defaultMessage: 'Area {index}'
  }
})
