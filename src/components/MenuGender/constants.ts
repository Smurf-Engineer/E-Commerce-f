/**
 * Home action types
 */

const namespace = 'src/MenuGender'

export const DEFAULT_ACTION = `${namespace}/DEFAULT_ACTION`
export const SET_SPORT_SELECTED = `${namespace}/SET_SPORT_SELECTED`
export const SET_CATEGORY_SELECTED = `${namespace}/SET_CATEGORY_SELECTED`
export const CLEAR_STATE_ACTION = `${namespace}/CLEAR_STATE_ACTION`
