/**
 * OverviewHeader -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  view: {
    id: 'components.OverviewHeader.view',
    defaultMessage: 'View'
  }
})
