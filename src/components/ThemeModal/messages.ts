/**
 * ThemeModal -  Messages
 */

import { defineMessages } from 'react-intl'

export default defineMessages({
  title: {
    id: 'components.ThemeModal.tittle',
    defaultMessage: 'ThemeModal'
  }
})